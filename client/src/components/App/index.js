// a naked webpage of what to upload the file import should look like.  dress it up for the page.
import React from 'react';
import './index.css';
import FileApp from './FileApp';

export default function App() {
  return ( <FileApp /> );
}